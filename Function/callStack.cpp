#include <iostream>
using namespace std;

int main()
{
    // Stack - a container which can have elements in the order of last in first out.
    // LIFO --- items are inserted from the top and also removed from the top
    // the inserted first will be removed at last
    return 0;
}
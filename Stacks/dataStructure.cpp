// Stack
// LIFO(Last In First Out)
// the first element will be removed at last and the  last element will be removed first
// we can remove data from top and also we can insert data from top only
// stack is a data structure that helps us to maintain this lifo kind of ordering
// for example- pile of plates
// we can implement stack using array or linked list or vector
// insertion(),pop(),top()(for viewing top most element), isempty(),isfull()